# Native Device Apis

Swift iOS development device SDK libraries are accessed usingthe UIKit framework.

## Screen Brightness

Screen brightness is a property that controls the illumination of the main screen. It is a float value that is between `0.0` and `1.0`, with `1.0` being the max brightness.
